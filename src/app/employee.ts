export interface Employee {
  id: number;
  name: string;
  email: string;
  jobTitle: string;
  phone: string;
  imageUrl: string;
  employeeCode: string;

}
//this represents the type of data that will be returning, it mirrors what is being shown for each Employee in the backend
